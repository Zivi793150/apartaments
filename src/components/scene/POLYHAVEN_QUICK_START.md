# Poly Haven HDRI - Быстрый старт

## 🚀 Самый быстрый способ начать

### Вариант 1: Использовать CDN (без скачивания)

Просто используйте готовый компонент - он уже настроен в `EstateBrowser3D.tsx`:

```tsx
// Уже работает! Просто запустите проект
npm run dev
```

HDRI загружается автоматически с CDN Poly Haven.

### Вариант 2: Скачать локально (лучше для продакшена)

1. Перейдите на [Poly Haven HDRI](https://polyhaven.com/hdris)
2. Найдите "City Street" или другую подходящую панораму
3. Скачайте в формате **HDR**, разрешение **4k**
4. Создайте папку `public/hdris/`
5. Поместите файл: `public/hdris/city_street_4k.hdr`
6. Обновите `EstateBrowser3D.tsx`:

```tsx
// Вместо CDN используйте локальный файл
const hdriUrl = "/hdris/city_street_4k.hdr";
```

## 🎨 Популярные HDRI для недвижимости

### Городские сцены (дневные):
- `city_street` - городская улица
- `urban_street` - урбанистическая улица  
- `downtown` - центр города
- `city_park` - городской парк

### Вечерние/ночные:
- `city_night` - город ночью
- `street_night` - улица ночью
- `city_sunset` - город на закате

### Природные:
- `forest` - лес
- `beach` - пляж
- `sunset` - закат

## 🔧 Настройка интенсивности

В `EstateBrowser3D.tsx` измените `hdriIntensity`:

```tsx
<BuildingScene3D 
  hdriUrl={hdriUrl}
  usePolyHaven={true}
  hdriIntensity={1.5}  // Ярче (день)
  // hdriIntensity={1.0}  // Нормально
  // hdriIntensity={0.7}  // Темнее (вечер)
/>
```

## 📝 Пример переключения между HDRI

```tsx
const [hdriName, setHdriName] = useState(POLYHAVEN_CITY_HDRI.cityStreet);

const hdriUrl = getPolyHavenHDRIUrl(hdriName, "4k");

<select onChange={(e) => setHdriName(e.target.value)}>
  <option value={POLYHAVEN_CITY_HDRI.cityStreet}>City Street</option>
  <option value={POLYHAVEN_CITY_HDRI.cityNight}>City Night</option>
  <option value={POLYHAVEN_CITY_HDRI.urbanStreet}>Urban Street</option>
</select>

<BuildingScene3D 
  hdriUrl={hdriUrl}
  usePolyHaven={true}
/>
```

## ✅ Преимущества Poly Haven HDRI

- ✅ **Бесплатно** - все HDRI бесплатны для коммерческого использования
- ✅ **Высокое качество** - до 16k разрешение
- ✅ **Реалистичное освещение** - естественный свет и отражения
- ✅ **Быстрая загрузка** - можно использовать CDN
- ✅ **Разнообразие** - сотни панорам на выбор

## 🎯 Что дальше?

1. Попробуйте разные HDRI из списка выше
2. Настройте интенсивность под вашу сцену
3. Для продакшена скачайте файлы локально

Готово! 🎉

