# Настройка Google Street View для 3D сцены

## ✅ Что уже сделано

1. ✅ Координаты здания добавлены: **36°46'23"N 4°02'20"W** (36.7731°, -4.0389°)
2. ✅ Интеграция Google Street View в `EstateBrowser3D.tsx`
3. ✅ Автоматическая загрузка панорамы при наличии API ключа

## 📋 Шаги для активации

### 1. Создайте файл `.env.local` в корне проекта

Создайте файл `.env.local` (если его еще нет) и добавьте ваш API ключ:

```env
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=ваш_ключ_здесь
```

**Важно:** 
- Файл `.env.local` уже должен быть в `.gitignore` (не коммитится в git)
- Перезапустите dev сервер после добавления ключа

### 2. Убедитесь, что API включен в Google Cloud Console

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Выберите ваш проект
3. Перейдите в "APIs & Services" → "Library"
4. Найдите "Street View Static API"
5. Убедитесь, что API **включен** (Enabled)

### 3. Перезапустите dev сервер

```bash
# Остановите текущий сервер (Ctrl+C)
# Затем запустите снова:
npm run dev
```

## 🎯 Как это работает

1. При загрузке страницы компонент автоматически:
   - Проверяет наличие API ключа
   - Генерирует URL панорамы Google Street View для координат вашего здания
   - Загружает 360° панораму как фон 3D сцены

2. Если API ключ не найден:
   - В консоли браузера появится предупреждение
   - Сцена будет использовать стандартное небо (градиент)

3. Панорама отображается как фон вокруг ваших 3D зданий, создавая реалистичное окружение

## 🔧 Настройка параметров

Если нужно изменить параметры панорамы, отредактируйте в `EstateBrowser3D.tsx`:

```tsx
return getGoogleStreetViewUrl(
  buildingLat,
  buildingLng,
  apiKey,
  "2048x1024", // Разрешение: можно изменить на "4096x2048" для лучшего качества
  90           // FOV (поле зрения): 90° для широкого обзора, можно 120° для еще шире
);
```

### Параметры heading и pitch

Если нужно изменить направление камеры или угол наклона, можно расширить функцию `getGoogleStreetViewUrl`:

```tsx
// В StreetViewEnvironment.tsx можно добавить параметры:
export function getGoogleStreetViewUrl(
  lat: number,
  lng: number,
  apiKey: string,
  size = "2048x1024",
  fov = 90,
  heading = 0,  // Направление: 0-360° (0 = север, 90 = восток, 180 = юг, 270 = запад)
  pitch = 0    // Угол наклона: -90 до 90° (0 = горизонтально, 90 = вверх)
): string {
  return `https://maps.googleapis.com/maps/api/streetview?size=${size}&location=${lat},${lng}&fov=${fov}&heading=${heading}&pitch=${pitch}&key=${apiKey}`;
}
```

## 🐛 Решение проблем

### Панорама не загружается

1. **Проверьте API ключ:**
   - Убедитесь, что ключ правильный в `.env.local`
   - Перезапустите dev сервер после изменения `.env.local`

2. **Проверьте, что API включен:**
   - В Google Cloud Console → APIs & Services → Library
   - Убедитесь, что "Street View Static API" включен

3. **Проверьте квоты:**
   - Google предоставляет 28,000 бесплатных запросов в месяц
   - Проверьте использование в Google Cloud Console → APIs & Services → Dashboard

4. **Проверьте координаты:**
   - Убедитесь, что для ваших координат доступна Street View
   - Можно проверить на [Google Maps](https://www.google.com/maps) - перетащите желтого человечка на карту

### Ошибка CORS

Если видите ошибку CORS, это нормально для Google Street View API - изображения загружаются напрямую, CORS не требуется.

### Панорама не видна

- Убедитесь, что `useStreetView={true}` передается в `BuildingScene3D`
- Проверьте консоль браузера на наличие ошибок
- Убедитесь, что панорама доступна для ваших координат

## 📊 Лимиты Google Street View API

- **Бесплатный тариф:** 28,000 запросов/месяц
- **Размер изображения:** до 640x640 (бесплатно) или до 2048x2048 (с биллингом)
- **FOV:** 10-120 градусов

Для продакшена рекомендуется:
- Кэшировать изображения
- Использовать CDN для хранения панорам
- Рассмотреть альтернативы (собственные 360° фото)

## 🎨 Альтернативы

Если Google Street View не подходит, можно использовать:

1. **Собственные 360° фотографии** - поместите в `public/panoramas/`
2. **Готовые панорамы** - скачайте с [Poly Haven](https://polyhaven.com/hdris)
3. **Mapbox** - для 3D карт с реальными зданиями

Все готово! После добавления API ключа в `.env.local` и перезапуска сервера, панорама должна автоматически загрузиться.

