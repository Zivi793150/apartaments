# 3D Визуализация здания в Испании с интерактивной выборкой квартир

## Резюме задачи

Нужно создать интерактивную 3D сцену здания в Испании, где:
- ✓ Пользователь может вращать 3D модель здания на 360°
- ✓ Видны окружающие объекты/окружение
- ✓ При наведении на квартиру она подсвечивается
- ✓ Можно кликнуть на квартиру для получения информации

**Имеющиеся ресурсы:**
- Адрес и координаты здания
- Планы квартир (public/plans)
- 3D визуализация здания от дизайнера
- Фото здания
- React Next.js проект с Three.js и Mapbox уже интегрированы

---

## Вариант 1: 🌟 **РЕКОМЕНДУЕМЫЙ - Гибридный подход (Panorama 360° + 3D модель)**

### Описание
Комбинирует 360° сферическую панораму с накладываемой 3D моделью здания:
1. **Фон:** 360° панорама окружения (снимаем с Google Street View или используем готовую)
2. **Главное:** 3D модель здания, загруженная как GLB/GLTF
3. **Интерактивность:** Вращение, выделение квартир при наведении

### Преимущества
✅ Реалистичное окружение  
✅ Высокий контроль над интерактивностью  
✅ Квартиры явно видны и выделяются  
✅ Подходит для веб-браузера  
✅ Уже есть инфраструктура Three.js/React Three Fiber в проекте  

### Недостатки
❌ Нужно конвертировать визуализацию в GLB/GLTF (требует 3D моделирования или конвертирования)  
❌ Требует точного позиционирования модели в сцене  

### Реализация

**Шаг 1: Подготовка модели**
```
Имея 3D визуализацию от дизайнера (вероятно, в формате .blend, .max, .c4d):
1. Открыть в Blender (бесплатно)
2. Экспортировать как GLB/GLTF
3. Оптимизировать (полигоны, текстуры)
4. Загрузить в public/models/building.glb
```

**Шаг 2: Получить 360° панораму**
```
Вариант А (Бесплатный): Google Street View
- Адрес на Google Maps
- Скачать панораму через Street View Download360

Вариант Б: Собственное фото
- Если есть 360° камера (Theta, Insta360)
- Или снять несколько фото и сшить в PTGui/Hugin

Вариант В: Готовая панорама
- Poly Haven (бесплатные HDRI с эффектом окружения)
```

**Шаг 3: Кодирование**
```tsx
// components/scene/SpainBuildingScene.tsx
import { Canvas } from "@react-three/fiber";
import { OrbitControls, Sphere } from "@react-three/drei";
import { useGLTF } from "@react-three/drei";

export default function SpainBuildingScene() {
  const building = useGLTF("/models/building.glb");
  
  return (
    <Canvas camera={{ position: [0, 5, 20], fov: 75 }}>
      {/* 360° Background panorama */}
      <Sphere args={[200, 32, 32]} scale={[-1, 1, 1]}>
        <meshBasicMaterial map={/* texture from /panoramas/street-view.jpg */} />
      </Sphere>
      
      {/* 3D Building model */}
      <primitive object={building.scene} scale={0.01} position={[0, 0, 0]} />
      
      {/* Interactive controls */}
      <OrbitControls enableDamping dampingFactor={0.05} />
    </Canvas>
  );
}
```

### Инструменты
- **Blender** (бесплатно) - экспорт модели
- **Street View Download360** - загрузка панорам
- **gltf-pipeline** - оптимизация модели
- **Three.js** (уже установлен) - рендеринг

### Стоимость
**$0** (все бесплатно)

---

## Вариант 2: 📹 **360° Видео с CSS/WebGL наложением**

### Описание
Проигрывание 360° видео (снятого/созданного видео окружения) с наложенной 3D моделью здания сверху.

### Преимущества
✅ Максимально реалистично  
✅ Окружение выглядит как "живое"  
✅ Простая интеграция видео  

### Недостатки
❌ Нужно снимать видео (дрон, экран записи + ручное композирование)  
❌ Большие файлы видео  
❌ Сложно делать динамичные эффекты выделения  

### Реализация
```
Получение видео:
1. Google Earth Pro - запись видео вращения вокруг адреса
2. ИЛИ Cesium.js - скачивание 3D моделей местности и запись
3. ИЛИ Drone видео + постобработка

Интеграция:
- HTML5 <video> элемент в canvas
- THREE.VideoTexture для фона
- 3D модель здания поверх видео
```

### Стоимость
**$50-500** (если нужен дрон или сервис) или **$0** если использовать бесплатные способы

---

## Вариант 3: 🗺️ **Mapbox 3D + Cesium (Профессиональный)**

### Описание
Использование Mapbox GL JS (уже в проекте!) с 3D layers и реальной топографией местности.

### Преимущества
✅ Реальная география и окружение  
✅ Встроенные 3D здания (если есть в OSM)  
✅ Масштабируемо  

### Недостатки
❌ Требует Mapbox API (платная)  
❌ Полностью реалистичный результат требует дополнительной подготовки  
❌ Сложнее кастомизировать выделение квартир  

### Стоимость
**$5-100/месяц** (Mapbox API)

---

## Вариант 4: 🖼️ **Matterport-подобный виртуальный тур**

### Описание
Создать серию 360° панорам на разных точках вокруг здания и соединить их в интерактивный тур.

### Преимущества
✅ Очень реалистично  
✅ Можно "ходить" вокруг здания  
✅ Легко добавлять горячие точки для квартир  

### Недостатки
❌ Требует много фотографий (10-20 панорам)  
❌ Нужно время на сшивание  
❌ Сложнее выделение квартир (работает через наложение)  

### Инструменты
- **Pannellum** (Open Source) - просмотрщик панорам
- **KRPANO** (платный, $99) - профессиональная альтернатива
- **Hugin** (бесплатный) - сшивание фотографий в панораму

### Стоимость
**$0-99**

---

## Вариант 5: 🎮 **Babylon.js с собственной 3D моделью**

### Описание
Более полнофункциональный движок для 3D, если нужны сложные физические симуляции или эффекты.

### Преимущества
✅ Мощнее Three.js  
✅ Лучше оптимизирована для мобильных  
✅ Встроенная поддержка физики  

### Недостатки
❌ Добавит зависимость  
❌ Новый стек (не установлен в проекте)  

### Стоимость
**$0** (бесплатно, Open Source)

---

## 🎯 **ФИНАЛЬНАЯ РЕКОМЕНДАЦИЯ**

### Лучший выбор: **Вариант 1 + Вариант 4 (гибрид)**

**Фаза 1 (быстрый MVP):** Вариант 1
- Использовать существующие Three.js компоненты
- Google Street View для панорамы
- GLB модель здания
- ≈ 1-2 недели разработки
- Стоимость: $0

**Фаза 2 (улучшения):** Добавить Вариант 4
- Если нужна большая реалистичность
- Создать несколько 360° точек вокруг здания
- Соединить в Pannellum для интерактивного тура
- ≈ 2-3 недели подготовки контента

---

## Пошаговый план реализации Варианта 1

### Этап 1: Подготовка модели (3-5 дней)

1. **Получить 3D файл от дизайнера**
   - Спросить в каком формате (Blender, 3DS Max, Cinema 4D, Revit и т.д.)

2. **Конвертировать в GLB/GLTF**
   ```bash
   # Если в Blender
   File > Export as > glTF 2.0 (.glb)
   ```

3. **Оптимизировать**
   ```bash
   # Установить gltf-pipeline
   npm install --save-dev @gltf-transform/cli
   
   # Сжать размер
   gltf-transform compress building.glb building-optimized.glb
   ```

4. **Загрузить в проект**
   ```
   public/models/building.glb
   ```

### Этап 2: Получить панораму (1-2 дня)

**Вариант А: Google Street View**
```
1. Перейти на Google Maps
2. Найти адрес здания
3. Использовать Street View Download 360
4. Скачать в 4096x2048 (хороший баланс качества/размера)
5. Загрузить в public/panoramas/street-view.jpg
```

**Вариант Б: Poly Haven (если нужна фантазийная среда)**
```
1. Перейти https://polyhaven.com/hdris
2. Выбрать подходящую HDRI
3. Скачать JPG 2K версию
4. Загрузить в public/panoramas/
```

### Этап 3: Создать компонент (3-5 дней)

```tsx
// src/components/scene/SpainBuildingViewer.tsx
"use client";
import React, { useRef, useState } from "react";
import { Canvas, useThree, ThreeEvent } from "@react-three/fiber";
import { OrbitControls, Sphere, useGLTF, useTexture } from "@react-three/drei";
import { Vector3 } from "three";

interface Apartment {
  id: string;
  floor: number;
  unit: number;
  area: number;
  rooms: number;
  position: [number, number, number];
}

export default function SpainBuildingViewer() {
  const [hoveredApt, setHoveredApt] = useState<string | null>(null);
  const [selectedApt, setSelectedApt] = useState<Apartment | null>(null);

  const apartments: Apartment[] = [
    { id: "A-1-1", floor: 1, unit: 1, area: 85, rooms: 2, position: [-5, 2, 0] },
    { id: "A-1-2", floor: 1, unit: 2, area: 110, rooms: 3, position: [5, 2, 0] },
    // ... остальные квартиры
  ];

  return (
    <div className="w-full h-screen bg-black">
      <Canvas camera={{ position: [0, 5, 20], fov: 75 }}>
        <Suspense fallback={null}>
          <Environment />
          <BuildingModel />
          <ApartmentMarkers 
            apartments={apartments}
            hoveredId={hoveredApt}
            onHover={setHoveredApt}
            onSelect={setSelectedApt}
          />
          <OrbitControls 
            enableDamping 
            dampingFactor={0.05}
            autoRotate
            autoRotateSpeed={2}
          />
          <ambientLight intensity={0.7} />
        </Suspense>
      </Canvas>

      {/* UI для информации о квартире */}
      {selectedApt && (
        <div className="absolute bottom-8 left-8 bg-white/90 p-4 rounded-lg">
          <h3>{selectedApt.id}</h3>
          <p>{selectedApt.area} м² • {selectedApt.rooms} комнаты</p>
        </div>
      )}
    </div>
  );
}

function Environment() {
  const texture = useTexture("/panoramas/street-view.jpg");
  return (
    <Sphere args={[200, 32, 32]} scale={[-1, 1, 1]}>
      <meshBasicMaterial map={texture} />
    </Sphere>
  );
}

function BuildingModel() {
  const { scene } = useGLTF("/models/building.glb");
  return <primitive object={scene} scale={0.01} />;
}

function ApartmentMarkers({ 
  apartments, 
  hoveredId, 
  onHover, 
  onSelect 
}: {
  apartments: Apartment[];
  hoveredId: string | null;
  onHover: (id: string | null) => void;
  onSelect: (apt: Apartment) => void;
}) {
  return (
    <group>
      {apartments.map(apt => (
        <ApartmentMarker 
          key={apt.id}
          apartment={apt}
          isHovered={hoveredId === apt.id}
          onHover={onHover}
          onSelect={onSelect}
        />
      ))}
    </group>
  );
}

function ApartmentMarker({ apartment, isHovered, onHover, onSelect }: any) {
  const ref = useRef<any>(null);

  return (
    <group position={apartment.position} ref={ref}>
      <mesh
        onPointerEnter={() => onHover(apartment.id)}
        onPointerLeave={() => onHover(null)}
        onClick={() => onSelect(apartment)}
      >
        <boxGeometry args={[0.5, 0.5, 0.5]} />
        <meshBasicMaterial 
          color={isHovered ? "#FF6A2B" : "#FFB88F"}
          opacity={isHovered ? 0.9 : 0.6}
          transparent
        />
      </mesh>
    </group>
  );
}
```

### Этап 4: Интеграция в приложение (2-3 дня)

```tsx
// src/app/building/page.tsx
import SpainBuildingViewer from "@/components/scene/SpainBuildingViewer";

export default function BuildingPage() {
  return (
    <main>
      <SpainBuildingViewer />
    </main>
  );
}
```

---

## Технологический стек

```
✓ Next.js 16 (уже установлен)
✓ React 19 (уже установлен)
✓ Three.js (уже установлен)
✓ React Three Fiber (уже установлен)
✓ Drei (уже установлен)
+ GLB/GLTF модели
+ Texture/Images для panoram
```

**Дополнительные библиотеки (опционально):**
```json
{
  "devDependencies": {
    "gltf-pipeline": "^2.0.0",
    "@gltf-transform/cli": "^1.0.0"
  }
}
```

---

## Примеры похожих сайтов

1. **Matterport** - https://www.matterport.com/ (профессиональное решение, платное)
2. **Zillow 3D** - https://www.zillow.com/ (использует собственные технологии)
3. **Sketchfab** - https://sketchfab.com/ (примеры интерактивных 3D моделей)
4. **KRPANO Examples** - https://krpano.com/examples/ (360° туры)

---

## Проблемы и решения

### Проблема 1: Модель не совпадает с панорамой по масштабу
**Решение:** Регулировать `scale` и `position` параметры в компоненте

### Проблема 2: Квартиры не видны с дальнего расстояния
**Решение:** Добавить billboard-логику для текстовых меток

### Проблема 3: Низкая производительность
**Решение:** Оптимизировать текстуры, использовать LOD (Level of Detail), включить GPU acceleration

### Проблема 4: Сложно выбирать квартиры
**Решение:** Добавить raycast-based selection (уже в Three.js)

---

## Итоговый чеклист

- [ ] Получить 3D файл от дизайнера
- [ ] Конвертировать в GLB в Blender
- [ ] Оптимизировать размер модели
- [ ] Загрузить модель в `public/models/`
- [ ] Получить 360° панораму (Google Street View или Poly Haven)
- [ ] Загрузить панораму в `public/panoramas/`
- [ ] Создать `SpainBuildingViewer.tsx` компонент
- [ ] Добавить список квартир с координатами
- [ ] Реализовать выделение квартир при наведении
- [ ] Тестирование на мобильных и десктопе
- [ ] Оптимизация производительности

---

**Вопросы для уточнения:**
1. ✅ В каком формате 3D модель от дизайнера?
2. ✅ Есть ли точные координаты каждой квартиры?
3. ✅ Нужны ли другие точки обзора кроме основного вращения?
4. ✅ Какие браузеры нужно поддерживать?

