# Mapbox 3D Визуализация здания в Испании

## ✅ Что было реализовано

Создана полнофункциональная 3D сцена здания в Испании с использованием **Mapbox GL JS**, включающая:

### Основные компоненты:
1. **`SpainBuilding3D.tsx`** - Основной компонент 3D сцены
   - Автоматическое вращение здания на 360° (20 секунд полный оборот)
   - 3D визуализация квартир с высотой этажей
   - Интерактивное выделение при наведении мыши
   - Информационные панели с деталями квартир
   - Фильтрация по количеству комнат и статусу

2. **`/spain-building/page.tsx`** - Страница демонстрации
3. **`/spain-building/client.tsx`** - Клиентский компонент с UI

### Функциональность:

✅ **360° вращение** - Карта автоматически вращается вокруг здания  
✅ **Интерактивность** - Выделение квартир при наведении  
✅ **Информация** - Всплывающие подсказки с данными квартир  
✅ **Фильтры** - Фильтрация по комнатам и статусу  
✅ **Статусы** - Цветовая разметка (свободна, зарезервирована, продана)  
✅ **Реальное окружение** - Показывает реальный ландшафт и здания вокруг  
✅ **3D terrain** - Рельеф местности с преувеличением  

---

## 🔧 Требуемая конфигурация

### 1. Mapbox API токен

Создайте `.env.local` в корне проекта:

```env
NEXT_PUBLIC_MAPBOX_TOKEN=pk.eyJ1IjoieW91cnVzZXJuYW1lIiwia....
```

**Как получить токен:**
1. Перейти на https://www.mapbox.com/
2. Зарегистрироваться (бесплатный аккаунт)
3. Перейти в Account → Tokens
4. Скопировать **Public token** (начинается с `pk.eyJ...`)

### 2. Координаты здания (опционально)

По умолчанию используются координаты Алгарробо, Испания:
```env
NEXT_PUBLIC_BUILDING_LAT=36.7696
NEXT_PUBLIC_BUILDING_LNG=-4.0387
```

Замените на координаты вашего здания:
- **LAT** - широта (около 36.7 для Испании)
- **LNG** - долгота (около -4.0 для Испании)

---

## 📍 Как использовать компонент

### Базовое использование

```tsx
import SpainBuilding3D from "@/components/scene/SpainBuilding3D";

export default function MyPage() {
  return (
    <SpainBuilding3D
      onPick={(apt) => console.log("Выбрана квартира:", apt)}
      showInfo={true}
    />
  );
}
```

### С фильтрами

```tsx
import { useState } from "react";
import SpainBuilding3D from "@/components/scene/SpainBuilding3D";

export default function MyPage() {
  const [filter, setFilter] = useState({
    rooms: null,          // 1, 2, 3 или null
    onlyAvailable: false  // показывать только свободные
  });

  return (
    <SpainBuilding3D
      filter={filter}
      onPick={(apt) => {
        console.log("ID:", apt?.id);
        console.log("Площадь:", apt?.area, "м²");
        console.log("Комнаты:", apt?.rooms);
        console.log("Статус:", apt?.status);
      }}
    />
  );
}
```

### Кастомные координаты

```tsx
<SpainBuilding3D
  center={[-4.0387, 36.7696]}  // [longitude, latitude]
  footprint={[
    [-4.0390, 36.7700],
    [-4.0385, 36.7700],
    [-4.0385, 36.7692],
    [-4.0390, 36.7692],
  ]}
  onPick={(apt) => console.log(apt)}
/>
```

---

## 📊 Структура данных квартиры

```typescript
type Spain3DPickedUnit = {
  id: string;           // "A-1-1" (здание-этаж-квартира)
  area: number;         // площадь в м²
  rooms: number;        // количество комнат (1, 2, 3)
  floor: number;        // номер этажа
  status: string;       // "available" | "reserved" | "sold"
} | null;
```

---

## 🎨 Цветовая схема

| Статус | Цвет | HEX |
|--------|------|-----|
| Свободная | Зелёная | #4fea98 |
| Зарезервирована | Жёлтая | #ffcd3c |
| Продана | Серая | #b8b8b8 |
| Выбранная | Оранжевая | #FF6A2B |

---

## 🚀 Запуск демонстрации

```bash
# Установить зависимости (если ещё не установлены)
npm install

# Запустить dev сервер
npm run dev

# Открыть http://localhost:3000/spain-building
```

---

## ⚙️ Параметры компонента

```typescript
interface SpainBuilding3DProps {
  center?: [number, number];           // [lng, lat] координаты центра
  footprint?: [number, number][];      // контур здания (4 точки)
  onPick?: (unit: Spain3DPickedUnit) => void;  // callback при клике
  filter?: Spain3DSceneFilter;         // фильтры
  showInfo?: boolean;                  // показывать инфо панель
}

type Spain3DSceneFilter = {
  activeBuilding?: "a" | "b";
  rooms?: 1 | 2 | 3 | 4 | null;
  onlyAvailable?: boolean;
  hoverFloor?: number | null;
};
```

---

## 🎬 Управление вращением

**Автоматическое вращение:**
- Вращается со скоростью 360° за 20 секунд
- Активируется по умолчанию при загрузке
- Можно отключить кнопкой в UI

**Ручное управление:**
- Перетаскивание мышью для поворота
- Скролл мыши для зума
- Правый клик + движение для наклона

---

## 📡 Технический стек

- **Next.js 16** - фреймворк
- **Mapbox GL JS** - 3D визуализация карты
- **Three.js** - (опционально для расширений)
- **Framer Motion** - анимации
- **Tailwind CSS** - стили
- **TypeScript** - типизация

---

## 🔗 Полезные ссылки

- [Mapbox Documentation](https://docs.mapbox.com/)
- [Mapbox GL JS API](https://docs.mapbox.com/mapbox-gl-js/)
- [Mapbox 3D Models](https://docs.mapbox.com/mapbox-gl-js/example/add-3d-models/)

---

## 🛠️ Возможные расширения

1. **Добавить собственные 3D модели зданий**
   - Использовать Babylon.js для загрузки GLTF моделей
   - Позиционировать поверх Mapbox сцены

2. **Интеграция с базой данных**
   - Загружать список квартир из БД
   - Динамически обновлять статусы

3. **Фотографии квартир**
   - Добавить карусель фото при выборе квартиры
   - Использовать 360° панорамы

4. **Виртуальный тур**
   - Комбинировать с Street View для окружения
   - Перемещение между разными точками

5. **Интеграция с чатом**
   - Рекомендации квартир в реальном времени
   - Бронирование прямо из сцены

---

## ❓ Часто задаваемые вопросы

**Q: Почему карта не отображается?**  
A: Проверьте наличие `NEXT_PUBLIC_MAPBOX_TOKEN` в `.env.local` и перезагрузите сервер.

**Q: Как изменить координаты?**  
A: Используйте параметры `center` и `footprint` компонента или обновите `.env.local`.

**Q: Можно ли использовать собственные 3D модели?**  
A: Да! Планируется расширение для загрузки GLTF/GLB моделей через Babylon.js.

**Q: Как добавить реальные данные квартир?**  
A: Замените генерацию данных в функции `generateApartmentData()` на запрос к вашему API.

---

## 📝 Разработка

Основные файлы компонента:

```
src/
├── components/
│   └── scene/
│       └── SpainBuilding3D.tsx         # Основной компонент
├── app/
│   └── spain-building/
│       ├── page.tsx                     # Серверная страница
│       ├── client.tsx                   # Клиентский компонент UI
│       └── layout.tsx                   # Layout
```

---

**Статус:** ✅ Готово к использованию  
**Последнее обновление:** Ноябрь 15, 2025  
**Версия:** 1.0.0
