# Варианты реализации 3D сцены здания в Испании

## Краткая сводка

Создано **два компонента** для отображения интерактивной 3D сцены здания:

### 1. `SpainBuildingScene3D` - для готовой GLB модели
- ✅ Использование готовой 3D модели от дизайнера
- ✅ Вращение на 360°
- ✅ Подсветка квартир при наведении

### 2. `SpainBuildingPhotoBased` - без готовой модели ⭐ (Рекомендуется)
- ✅ Простая геометрия на основе планов
- ✅ Использование фотографий как текстур
- ✅ Интеграция с Mapbox для окружения
- ✅ Вращение на 360°
- ✅ Подсветка квартир при наведении

## Варианты реализации

### 1. Фото + Простая геометрия + Mapbox ⭐ (Рекомендуется)

**Что нужно:**
- Фотографии фасада (опционально)
- Планы этажей для размеров
- Mapbox API ключ (опционально, для окружения)

**Преимущества:**
- ✅ Не требует 3D модели
- ✅ Быстро реализовать
- ✅ Использует существующие ресурсы
- ✅ Реалистичное окружение через Mapbox

**Использование:**
```tsx
import SpainBuildingPhotoBased from "@/components/scene/SpainBuildingPhotoBased";

<SpainBuildingPhotoBased
  apartments={apartmentsConfig}
  coordinates={{ lat: 36.7731, lng: -4.0396 }}
  address="C. Cam. de Velez, 15, Альгарробо"
  useMapbox={true}
  onPick={(unit) => console.log("Selected:", unit)}
/>
```

**См. также:** `docs/SPAIN_3D_ALTERNATIVES.md` для полного списка вариантов

---

### 2. Готовая GLB модель

**Что нужно:**
- Модель здания в формате GLB/GLTF
- Модель должна быть в `public/models/building.glb`
- Квартиры должны быть названы в модели (например: `apartment_1_1`, `apartment_2_3`)

**Преимущества:**
- Не требует моделирования через код
- Использует готовую модель от дизайнера
- Высокое качество визуализации

**Использование:**
```tsx
import SpainBuildingScene3D from "@/components/scene/SpainBuildingScene3D";

<SpainBuildingScene3D
  apartments={apartmentsConfig}
  coordinates={{ lat: 36.7731, lng: -4.0396 }}
  address="C. Cam. de Velez, 15, Альгарробо"
  onPick={(unit) => console.log("Selected:", unit)}
/>
```

### 2. Панорамное окружение

**Варианты:**
- Google Street View панорамы (требует API ключ)
- Собственные 360° фотографии
- Готовые панорамы из Poly Haven / HDRI Haven

**Использование:**
```tsx
<SpainBuildingScene3D
  usePanorama={true}
  panoramaUrl="/images/panoramas/spain-360.jpg"
  apartments={apartmentsConfig}
/>
```

### 3. Конфигурация квартир

**Структура:**
```typescript
{
  id: "66-A001",
  name: "Flat 66-A001",
  area: 39.06,
  rooms: 2,
  floor: 1,
  available: true,
  meshName: "apartment_1_1" // Должно совпадать с именем в модели
}
```

**Источники данных:**
- JSON файл
- API
- Генерация из планов в `public/plans/`

### 4. Интеграция с планами

Используйте планы из `public/plans/` для:
- Определения позиций квартир
- Валидации конфигурации
- Отображения в tooltip

## Что уже готово

✅ Компонент `SpainBuildingPhotoBased` - без готовой модели  
✅ Компонент `SpainBuildingScene3D` - для GLB модели  
✅ Поддержка загрузки GLB моделей  
✅ Простая геометрия на основе планов  
✅ Интеграция с Mapbox для окружения  
✅ Интерактивность (наведение, клик)  
✅ Подсветка квартир при наведении  
✅ Вращение на 360°  
✅ Фильтрация квартир  
✅ Tooltip с информацией о квартире  
✅ Поддержка панорамного окружения  

## Что нужно сделать

### Для варианта с фото (Рекомендуется):

1. **Настроить конфигурацию квартир:**
   - Отредактировать `DEFAULT_APARTMENTS` в `SpainBuildingPhotoBased.tsx`
   - Указать позиции и размеры квартир на основе планов

2. **Опционально - добавить фотографии:**
   - Подготовить фотографии фасада
   - Разместить в `public/images/building/`
   - Раскомментировать код загрузки текстур

3. **Опционально - настроить Mapbox:**
   - Добавить `NEXT_PUBLIC_MAPBOX_TOKEN` в `.env.local`
   - Установить `useMapbox={true}`

### Для варианта с GLB моделью:

1. **Подготовить модель:**
   - Экспортировать модель в GLB формат
   - Убедиться, что квартиры названы правильно
   - Разместить в `public/models/building.glb`

2. **Настроить конфигурацию квартир:**
   - Создать массив `ApartmentConfig` с данными квартир
   - Указать `meshName` для каждой квартиры (должно совпадать с именем в модели)

## Файлы

- `src/components/scene/SpainBuildingPhotoBased.tsx` - компонент без готовой модели ⭐
- `src/components/scene/SpainBuildingScene3D.tsx` - компонент для GLB модели
- `docs/REALITYSCAN_GUIDE.md` - пошаговая инструкция по RealityScan 📱
- `docs/PHOTOGRAMMETRY_COMPARISON.md` - сравнение инструментов фотограмметрии
- `docs/SPAIN_3D_ALTERNATIVES.md` - все варианты реализации
- `docs/SPAIN_3D_QUICK_START.md` - быстрый старт
- `docs/SPAIN_BUILDING_3D_SETUP.md` - подробная документация
- `src/components/scene/SPAIN_BUILDING_EXAMPLE.md` - примеры использования

## Следующие шаги

1. **Выберите вариант:**
   - Без модели → используйте `SpainBuildingPhotoBased`
   - С моделью → используйте `SpainBuildingScene3D`

2. **Настройте конфигурацию:**
   - Отредактируйте `DEFAULT_APARTMENTS` под ваши данные
   - Укажите позиции и размеры на основе планов

3. **Используйте компонент:**
   ```tsx
   import SpainBuildingPhotoBased from "@/components/scene/SpainBuildingPhotoBased";
   ```

4. **См. примеры:**
   - `docs/SPAIN_3D_QUICK_START.md` - быстрый старт
   - `src/components/scene/SPAIN_BUILDING_EXAMPLE.md` - примеры

## Поддержка

Если квартиры не подсвечиваются:
- Проверьте, что `meshName` совпадает с именем меша в модели
- Убедитесь, что материалы поддерживают `emissive`
- Проверьте консоль браузера на ошибки

Если модель не загружается:
- Проверьте путь к файлу (`public/models/building.glb`)
- Убедитесь, что формат GLB (не GLTF)
- Проверьте размер файла (оптимизируйте если слишком большой)

